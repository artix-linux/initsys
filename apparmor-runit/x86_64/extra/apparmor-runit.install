post_install()
{
    echo "==> Please note that by default, the AppArmor runit service"
    echo "    doesn't load any profile on any mode. You will want to"
    echo "    edit the behavior in /etc/rc/apparmor.conf"
}
